#include "primos_header.h"
#include "primos_dev.h"

int main(){

	loop(get_nun());
 	replay();
  
	return 0;
}

// Este código é capaz de verificar 
// se os antecessores de um dado numero 
// são ou não primos.
// Para compilar este código, pode ser usado o seguinte // comando no terminal linux: gcc -Wall Primos_main.c

// Para execução do mesmo, use: ./a.out 
// Este código está dividido em 3 arquivos. 
// primos_header.h, primos_dev.h e Primos_main.c. 

// Sendo protótipos de função e código de 
// préprocessamento, desenvolvimento das funções e sua 
// utilização respectivamente em cada arquivo.
